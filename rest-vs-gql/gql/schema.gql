type Mutation {
  seed: Boolean!
}

type Query {
  users: [User!]!
}

type Task {
  desc: String
  id: ID!
  title: String
  users: [User!]!
}

type User {
  email: String
  id: ID!
  name: String
  tasks: [Task!]!
}