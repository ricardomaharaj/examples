{
  "name": "server",
  "module": "src/server.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "postinstall": "prisma generate",
    "gen": "prisma generate",
    "push": "prisma db push",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@fastify/cors": "^9",
    "@prisma/client": "^5",
    "@types/bun": "^1",
    "fastify": "^4",
    "prisma": "^5",
    "typescript": "^5"
  }
}
